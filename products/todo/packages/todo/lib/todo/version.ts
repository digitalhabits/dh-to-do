/**
 * Product version of "Digital Habits: To-Do" — independent of the planner's
 * own version. Shown in the settings modal in both the planner tab and the
 * (future) standalone app; release tooling stamps it into apps/todo's
 * tauri.conf.json. The shipped vanilla app is 2.9.0, so the React rewrite
 * lives on the 3.x line (updaters and app stores require going upward).
 */
export const TODO_APP_VERSION = "3.0.0-alpha.1";
